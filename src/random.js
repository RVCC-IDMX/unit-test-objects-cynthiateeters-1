/**
 * Returns a random integer between min (inclusive) and max (exclusive)
 * @param {number} min - integer of the minimum value to be returned
 * @param {number} max - integer of the maximum value to be returned
 * @returns {number} - random integer between min and max
 * example: getRandomInt(1, 10) => random integer between 1 and 9
 * example: getRandomInt(1, 1) => 1
9 */
function getRandomInt(min, max) {
  // write your code here & return value
}

export default getRandomInt;
